
# Serving stack (docker compose up -d). The pipeline reads these; defaults match docker-compose.yml.
LR_PG_DSN=postgresql://lr:lr@127.0.0.1:5432/lr
LR_S3_ENDPOINT=http://127.0.0.1:9000
LR_S3_KEY=lr
LR_S3_SECRET=lr-minio-secret
LR_REDIS_URL=redis://127.0.0.1:6379/0
